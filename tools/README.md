**TOOLS**

Internal tooling and dev-only utilities for the monorepo.

**-** `tools/harness/` is the dev/test harness used to run smoke/EC tests.
**-** `tools/*.neon`, `tools/*.xml`, `tools/*.php` hold shared QA configs (phpstan, psalm, rector).

Tools are not part of the public starter app.
